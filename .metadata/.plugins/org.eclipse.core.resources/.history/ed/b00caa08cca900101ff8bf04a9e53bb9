server:
 port: 8080
 servlet:
  context-path: /api

spring:
 datasource:
  url: jdbc:hsqldb:mem:customersdb
  driver-class-name: org.hsql.jdbcDriver
  username: sa
  password:
 jpa: 
  hibernate:
   ddl-auto: update
  show-sql: true

app:
   jwt:
    secret: ${JWT_SECRET:dev-secret-change-me}
   internal:
    secret: ${INTERNAT_SECRET:dev-internal-secret}
		